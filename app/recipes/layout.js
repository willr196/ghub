export const metadata = {
  title: 'Healthy Recipes',
  description: 'Simple, nutritious recipes to support training and recovery.',
}

export default function RecipesLayout({ children }) {
  return children
}
