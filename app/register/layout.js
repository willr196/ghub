export const metadata = {
  title: 'Create Account',
  description: 'Create your GHUB account with a registration code.',
  robots: {
    index: false,
    follow: false,
  },
}

export default function RegisterLayout({ children }) {
  return children
}
