export const metadata = {
  title: 'Fitness Science',
  description: 'Evidence-based training, recovery, and nutrition insights from GHUB.',
}

export default function ScienceLayout({ children }) {
  return children
}
