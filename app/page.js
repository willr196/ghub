import HomeClient from '@/components/HomeClient'

export const metadata = {
  title: 'Personal Fitness & Wellness Hub',
  description: 'Track workouts, goals, sobriety milestones, and daily wellness in one private fitness hub.',
}

export default function Home() {
  return <HomeClient />
}
