export const metadata = {
  title: 'Fitness Journey Blog',
  description: 'Reflections, wins, and lessons learned from the GHUB journey.',
}

export default function BlogLayout({ children }) {
  return children
}
